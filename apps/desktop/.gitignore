# Tauri updater signing keys must NEVER be committed. The PUBLIC key lives in
# src-tauri/tauri.conf.json (plugins.updater.pubkey); the PRIVATE key + password go into CI
# secrets only. See UPDATER-SETUP.md.
*.key
*.key.pub

# Playwright/e2e build output (not source).
test-results/
playwright-report/
